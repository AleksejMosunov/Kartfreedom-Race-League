version: "3.9"

services:
  frontend:
    image: ghcr.io/aleksejmosunov/kartfreedom-race-league:latest
    container_name: kartfreedom-app
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - /root/Kartfreedom-Race-League/.env
