Place sponsor images here.

- Use filenames referenced in code (e.g. `redbull.png`).
- Prefer optimized PNG or SVG images.
- For local development you can add SVGs; for production use real image assets.
